version: '3'
services:
  face-recognition-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: face-recognition-api
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    restart: unless-stopped
