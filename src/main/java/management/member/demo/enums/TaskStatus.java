package management.member.demo.enums;

public enum TaskStatus {
    NEW,        // New task (maps to "new" in Frontend)
    PENDING,    // Pending task (maps to "pending" in Frontend)
    IN_PROGRESS,
    REVIEW,// In progress task (maps to "in-progress" in Frontend)
    COMPLETED,   // Completed task (maps to "complete" in Frontend)
    CANCELLED    // Cancelled task
}
